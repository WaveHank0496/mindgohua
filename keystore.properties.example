# Release 簽章設定的範本。
#
# 使用方式：把這個檔案複製一份、改名成 keystore.properties，填入你自己的值。
# keystore.properties 已被 .gitignore 排除，不會進版控；這份 .example 才會。
#
# ⚠️ 這四個值等同於 app 的身分證。金鑰檔或密碼遺失，
#    這個 app 就永遠無法再發布更新 —— Play 認的是簽章，不是你的帳號。
#    請把 .jks 檔與密碼另外備份到安全的地方（密碼管理器 / 離線備份）。
#
# 產生金鑰的指令（在專案根目錄執行，Windows PowerShell）：
#
#   keytool -genkeypair -v `
#     -keystore mindgohua-release.jks `
#     -alias mindgohua `
#     -keyalg RSA -keysize 4096 -validity 10000
#
# 它會問你密碼與一些身分資訊（姓名、組織、城市、國家代碼填 TW）。
# 身分資訊不會顯示給使用者，照實填即可。

# .jks 檔相對於專案根目錄的路徑
storeFile=mindgohua-release.jks

# keystore 本身的密碼
storePassword=在這裡填入你設定的密碼

# 金鑰別名，要跟上面指令的 -alias 一致
keyAlias=mindgohua

# 金鑰的密碼（keytool 允許與 storePassword 相同）
keyPassword=在這裡填入你設定的密碼
