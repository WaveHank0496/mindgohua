# 怎麼測這個 app（從沒寫過手機 app 也能跟著做）

開發環境為 Windows + OPPO Reno7。以下指令都以 **PowerShell** 為準。

---

## 先建立心智模型：測試有三層，由快到慢

寫 app 跟寫別的程式最大的差別是「跑一次很貴」——編譯 + 安裝 + 手動點按可能要 3 分鐘。
所以測試要分層，**能在上一層抓到的錯就不要拖到下一層**。

| 層 | 測什麼 | 要不要手機 | 一輪多久 |
|---|---|---|---|
| 1. 單元測試 | 計時、冷卻、門檻、GRACE、滑動節奏演算法 | ❌ 不用 | ~30 秒 |
| 2. 手動功能測試 | 權限、overlay 長相、按鈕、通知 | ✅ 要 | ~2 分鐘 |
| 3. 存活測試 | ColorOS 會不會殺掉服務 | ✅ 要，且要放著 | 數小時 |

這個專案的核心邏輯（`SessionEngine`、`ScrollRhythmAnalyzer`）**刻意不依賴 Android**，
就是為了讓最容易出錯的部分留在第 1 層。

---

## 第 0 步：裝環境

1. 下載安裝 [Android Studio](https://developer.android.com/studio)（自帶 JDK 與 Android SDK，不用另外裝）。
2. 開啟 Android Studio → `Open` → 選本專案資料夾。
3. 右下角會跑 **Gradle Sync**（第一次要下載一堆東西，可能十幾分鐘）。
   同步完成前紅字都不用理。
4. 同步完如果還有紅字，那是真的錯誤，看 `Build` 分頁的訊息。

### 把 adb 加進 PATH（之後會一直用到）

`adb` 是電腦跟手機溝通的工具。它在：

```
C:\Users\user\AppData\Local\Android\Sdk\platform-tools\adb.exe
```

每次都打全路徑很煩，先在 PowerShell 設一個捷徑（只對當前視窗有效）：

```powershell
$env:Path += ";$env:LOCALAPPDATA\Android\Sdk\platform-tools"
```

---

## 第 1 層：單元測試（先做這個，不用手機）

在 Android Studio 的下方 `Terminal` 分頁輸入：

```powershell
.\gradlew.bat :app:testDebugUnitTest
```

應該看到 `BUILD SUCCESSFUL`。這一步驗證了：

- 連續使用超過門檻會打斷
- 短暫切出再切回**不會**把計時洗掉（冷卻邏輯）
- 超過冷卻回來會開新 session（反例）
- 解除後 GRACE 期內不再打斷
- 滑動節奏演算法的統計算得對
- manifest 沒有偷偷加上網路權限

看詳細報告（哪個測試過、哪個沒過）：

```powershell
start app\build\reports\tests\testDebugUnitTest\index.html
```

**這一層失敗就先別碰手機。** 邏輯錯在手機上只會更難查。

---

## 第 2 層：裝到手機上

### 2.1 打開手機的開發者模式

OPPO / ColorOS 的路徑：

1. 設定 → 關於手機 → **版本資訊** → 連點「版本號」7 次
2. 回到 設定 → 系統設定 → 開發者選項
3. 打開 **USB 偵錯**

只要這一項就夠了。網路上很多教學會叫你另外開「USB 安裝」，
但那個選項在不同 ColorOS 版本名稱不一，有時根本找不到 ——
**實測 Reno7 上沒開它也能正常 `installDebug`**，所以找不到不用管。

線材方面，Type-C 對 Type-C 沒問題，接頭形狀不重要，
重點是那條線要能傳資料（很多附贈的充電線只供電）。

### 2.2 接上線確認電腦看得到手機

```powershell
adb devices
```

第一次接會在手機上跳出「允許 USB 偵錯嗎」→ 勾「一律允許」→ 確定。

看到類似這樣就對了：

```
List of devices attached
ABC123XYZ       device
```

如果顯示 `unauthorized`，就是沒按手機上那個確認框。
如果什麼都沒有，換一條線試試（很多充電線只有供電沒有資料）。

### 2.3 安裝

```powershell
.\gradlew.bat installDebug
```

或在 Android Studio 上方選好裝置後按綠色 ▶️。

---

## 第 3 步：給權限

App 裡的「權限」區塊每一項都有「去開」按鈕，跟著點就好。

> ⚠️ **ColorOS 擋掉了 adb 授權捷徑。**
> 在多數 Android 裝置上可以用 `adb shell appops set … allow` 和 `adb shell pm grant …`
> 一行給完權限，省去點選單。但 OPPO 在 shell 帳號上拔掉了
> `MANAGE_APP_OPS_MODES` 與 `GRANT_RUNTIME_PERMISSIONS`，
> 在這台 Reno7（實測 Android 13 / CPH2371）上會直接噴 `SecurityException`。
>
> 所以**權限一律用 app 內的「去開」按鈕手動給**。重裝 app 通常不會清掉已給的權限，
> 只有 `adb uninstall` 或 `pm clear` 之後才要重來一次。

**無障礙服務（只有 Mode B 需要）** 比較特別，建議手動開：

設定 → 系統設定 → 無障礙 → 已下載的服務 → Cat Gatekeeper → 開啟

> 也可以用 adb 開，但那個設定值是「用冒號分隔的清單」，
> 直接寫會蓋掉你原有的無障礙服務。除非你確定清單是空的，否則不要用：
> ```powershell
> adb shell settings put secure enabled_accessibility_services com.catgatekeeper/com.catgatekeeper.service.ScrollWatchService
> adb shell settings put secure accessibility_enabled 1
> ```

### 順手驗證「真的沒有網路權限」

```powershell
adb shell dumpsys package com.catgatekeeper | Select-String "INTERNET"
```

**應該什麼都找不到。** 找到了就是出事了。
（已在 Reno7 實機驗證通過。）

---

## 第 4 步：Mode A 驗收（對應 spec §8）

**先把門檻調小**，不然每測一輪要等 2 分鐘。在 app 設定頁：

- 連續使用超過 → **30 秒**
- 切出去多久內回來算同一段 → **10 秒**
- 解除後多久內不再打斷 → **1 分鐘**
- 貓出現後幾秒才能按 → **3 秒**

然後逐條測：

| # | 怎麼做 | 應該看到 |
|---|---|---|
| 1 | 勾選 Instagram、打開最上面的開關 | 通知欄出現常駐通知「貓在待命」 |
| 2 | 開 IG 滑 30 秒 | 貓蓋滿整個螢幕 |
| 3 | 貓出現後 3 秒內狂點按鈕 | **點不動**，下方顯示倒數 |
| 4 | 按「停下來」 | 回到桌面，貓消失 |
| 5 | 再開 IG 滑 30 秒 | **不會出現貓**（還在 GRACE 期） |
| 6 | 等 1 分鐘後再滑 30 秒 | 貓又出現了 |
| 7 | 長按「再滑 N 分鐘」 | 按鈕顯示 0%→100%，放開太早就取消 |
| 8 | 開 IG 滑 20 秒 → 切到別的 app 5 秒 → 切回 IG 滑 10 秒 | **貓出現**（累計 30 秒，計時沒被洗掉）|
| 9 | 開 IG 滑 20 秒 → 切出去 30 秒 → 切回 IG 滑 10 秒 | **貓不出現**（超過冷卻，重新計時）|
| 10 | 滑 IG 到一半按電源鍵關螢幕，等 1 分鐘再解鎖 | 解鎖瞬間**不該**跳出貓 |

第 8、9 條是最容易出錯的地方（session 邊界），也是單元測試已經覆蓋的部分——
如果第 1 層過了但這裡沒過，問題出在偵測層而不是計時邏輯。

不想等門檻也可以用設定頁最下面的 **「預覽『待太久』的貓」** 按鈕，直接看 overlay 長相。

---

## 第 5 步：Mode B 驗收

Mode B 比 Mode A 難測，因為它有**三段**可能斷掉。設定頁的「Mode B 驗收」卡片
就是照這三段排的，壞了才知道壞在哪：

```
無障礙服務有沒有連上  →  事件有沒有進來  →  統計量有沒有跨過門檻
      (第一段)              (第二段)              (第三段)
```

### 操作流程

1. 設定頁把偵測方式切到 **Focus Mode**
2. 去系統設定開無障礙服務，回來看第一段的兩個 ✔ 是不是都亮了
3. 打開最上面的開關
4. **打開 IG 隨便滑 10 下**，回到本 app
5. 看第二段：「已從目標 app 收到 N 個滑動事件」——

| N 的值 | 代表什麼 | 怎麼辦 |
|---|---|---|
| 0 | 管線斷了 | 檢查無障礙服務是否被 ColorOS 關掉；重開服務 |
| > 0 | 管線通了 ✅ | 繼續看第三段 |

6. 第三段會顯示四個指標，每個都標了「目前值 vs 門檻」，
   **綠色代表這一項過了**。四項全過並維持足夠久才會觸發。

> 離開 IG 回到設定頁時，數字會**停在你離開前的最後一筆**（畫面上會註明），
> 不是歸零。這是刻意的——不然你回來只會看到一片 0，根本沒辦法調。

### 真正要觸發一次

門檻預設要「持續 60 秒」，測試時先調小：

- 需持續多久才觸發 → **15 秒**

然後開 IG 用**穩定的節奏**連續滑 30 秒（不要停下來看內容，就是機械式地滑）。
應該會跳出貓，文案是「你已經無意識地滑了一陣子了」。

### 一邊滑一邊看數字（調參用）

這是 Mode B 最實用的功能：打開「**把數字顯示在常駐通知上**」。

然後開 IG，**下拉通知欄**就能看到：

```
密度 42/分 · CV 0.31 · 符合 18s · 觸發 0
```

這樣你可以邊滑邊觀察自己的數字。做兩組對照：

| 情境 | 你要記下的數字 |
|---|---|
| **故意無意識刷**（機械式狂滑，不看內容） | 密度、CV 各是多少 |
| **正常認真用**（看貼文、回訊息、想一下） | 密度、CV 各是多少 |

門檻要設在**兩組之間**。例如無意識刷是密度 45 / CV 0.3，
認真用是密度 20 / CV 0.9，那密度門檻設 30、CV 門檻設 0.5 就是合理起點。

調完記得把診斷通知關掉（它每 2 秒重畫一次，會耗電）。

> ⚠️ 模擬中發現一件事值得注意：「認真但滑很快」的使用模式，
> **密度是會超過門檻的**（模擬值 18.7/分），真正把它擋下來的是變異係數（1.26）。
> 所以**兩個門檻要一起調，不要只調密度**——只看密度會把認真的人也打斷。

---

## 第 5.5 步：每日滑動計數

這個功能不在原始 spec 裡，而且它**放寬了 spec §0.1 的「不落地」**，
所以驗收時除了「數字對不對」，也要驗「存進去的到底是什麼」。

### 數字對不對

1. Focus Mode + 無障礙服務已開
2. 「每日滑動計數」卡片 → 打開兩個開關
3. 開 IG → Reels → 數著滑 10 部
4. 右上角 `▽` 的數字應該同步跳，且回到 app 後「今天：N 項」與你滑的數量吻合

若出現紅字「這個 app 的滑動事件沒有附上清單位置資訊」，代表那個 app
不填 `fromIndex`/`toIndex`/`itemCount`，數不出來 —— **這是誠實的失敗，不是 bug**。

### 存進去的是什麼（自己查，不要只信文件）

debug build 可以用 `run-as` 讀 app 私有目錄：

```powershell
adb shell run-as com.catgatekeeper ls -l files/datastore/
adb shell "run-as com.catgatekeeper cat files/datastore/cat_gatekeeper_daily_stats.preferences_pb"
```

輸出裡應該只有 `c|日期|package` 這種 key 加上整數。
**看到任何時間戳、URL、文字內容都是出事了。**

實測結果（Reno7）：整個歷史檔 42 bytes，內容為
`c|2026-07-23|com.instagram.android`。

### 換日邊界

把「每天幾點歸零」設成**目前時間的下一個整點**，然後等跨過那個時間點再滑幾下，
「今天」的數字應該重新從 0 起算，前一天的數字進入歷史列表。

實測順帶驗到：凌晨 01:00 滑的內容，在預設 4:00 換日設定下正確記入前一天。

---

## 第 6 步：ColorOS 存活測試（最重要也最花時間）

這是 spec 列的驗收關鍵指標，也是這類 app 最常見的失敗原因：
**服務被系統殺掉，而且不會給你任何提示**，你只會發現貓再也沒出現過。

### 前置設定（app 內「讓貓活著」區塊有按鈕帶過去）

1. 設定 → 電池 → Cat Gatekeeper → **不受限制 / 允許背景執行**
2. 手機管家 / 安全中心 → **自啟動管理** → 允許 Cat Gatekeeper
3. 最近任務畫面把本 app **上鎖**（下拉卡片會出現鎖頭圖示）

### 測試方法：用 app 內的「存活紀錄」，不要用 adb

> ⚠️ **測試期間必須拔掉 USB 線。**
> ColorOS 殺後台最兇的時機是「拔電源 + 螢幕關閉 + 靜置」進入深度休眠時。
> 插著電手機根本不會進那個狀態，測出來的存活時間是假的。
>
> 這也表示 **adb 在這個測試裡派不上用場** —— 你不能一邊拔線一邊查 PID。
> 所以服務改成自己每分鐘蓋一次時間戳（`SurvivalLog`），
> 被殺掉時時間戳就停住，下次啟動一比對就知道死了多久。
> **觀測者不介入被觀測的系統。**

流程：

1. 設定頁 →「存活紀錄」→ 按 **重新開始記錄**（歸零基準）
2. **拔掉 USB 線**
3. 正常用手機，或直接放著過夜
4. 隔天打開 app，看「存活紀錄」卡片

### 怎麼讀結果

| 卡片顯示 | 意思 |
|---|---|
| 「已連續存活 7 小時 30 分」+「尚未偵測到任何中斷」 | ✅ **通過** |
| 「已連續存活 0 分」+「曾中斷 1 次，最近一次中斷了 5 小時」 | ❌ 被殺了，而且知道死多久 |
| 中斷但標註「重開機造成」 | 不算失敗，重開機本來就會中斷 |

打開 app 時服務會自動接回來，所以早上開來看不會破壞紀錄 ——
中斷事件在服務重新啟動的那一刻就已經被記下來了。

### 要測三種情境（失效模式不同）

| 情境 | 觀察重點 |
|---|---|
| 螢幕開著、用其他 app | 幾乎不會被殺，這是基準線 |
| 螢幕關閉 1 小時 | ColorOS 深度睡眠，主要殺手 |
| 螢幕關閉過夜（8 小時） | 最嚴苛，多半要有自啟動白名單才過得了 |

### 通過條件

螢幕關閉 + 切換多個 app 後**連續存活 ≥ 4 小時**，且期間開 IG 滑超過門檻貓仍會出現。

---

## 出事的時候怎麼查

### 看 log

```powershell
adb logcat -s GatekeeperService:* UsageStatsDetector:* ScrollWatchService:* BootReceiver:*
```

保持這個視窗開著，然後去操作手機，就能看到程式在做什麼。

### 看 app 有沒有當掉

```powershell
adb logcat -d | Select-String -Pattern "catgatekeeper" -Context 5
```

當掉的話會有一大段 `FATAL EXCEPTION`，
最上面幾行會直接指出是哪個檔案哪一行。

### 確認 foreground service 還在

```powershell
adb shell dumpsys activity services com.catgatekeeper
```

### 重新開始（清掉所有設定）

```powershell
adb shell pm clear com.catgatekeeper
```

---

## 常見卡關

| 症狀 | 原因 |
|---|---|
| `adb devices` 空的 | USB 線只有供電沒資料，或沒開 USB 偵錯 |
| 裝不上去，說安裝被拒 | ColorOS 的「USB 安裝」開關沒開 |
| 開關打不開（灰色） | 上面顯示「還缺：⋯」，缺的權限還沒給 |
| 貓不出現 | 先按「預覽」確認 overlay 本身沒問題；再看通知有沒有在計時 |
| 貓出現在權限設定畫面上「蓋不住」 | 這是 Android 的保護機制，正常，不用修 |
| 放一陣子就完全沒反應 | ColorOS 殺後台，做第 6 步的前置設定 |
| Mode B 事件數一直是 0 | 無障礙服務被系統關掉了（ColorOS 常幹這事），重開 |

---

## 一句話總結流程

```
改程式碼 → 跑單元測試 → 裝到手機 → 用「預覽」看 UI → 調小門檻做功能測試 → 放著跑存活測試
```

前三步失敗就別往下走，會浪費很多時間。
